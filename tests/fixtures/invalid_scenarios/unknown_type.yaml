version: "1.0"
target:
  type: skill
  path: skills/newsletter
  trigger: /send-newsletter
scenarios:
  - name: bad-assertion
    when:
      invoke: /send-newsletter Sunday Service
    then:
      - type: made_up_check
